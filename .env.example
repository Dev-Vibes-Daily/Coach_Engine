# Copy this file to .env.local and add your real Anthropic API key.
# .env.local is git-ignored and never committed. The key is attached
# server-side by the Vite dev proxy and never reaches the browser.

VITE_ANTHROPIC_KEY=sk-ant-your-key-here
